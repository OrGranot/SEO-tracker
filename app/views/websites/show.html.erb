<%= turbo_frame_tag("content") do %>
  <% role = @website.user == current_user || @website.users.include?(current_user) ? 'owner' : 'viewer' %>
  <% role = current_user.role(@website.id) %>
  <%= render 'shared/table_card', website: @website, role: role %>
<% end %>
